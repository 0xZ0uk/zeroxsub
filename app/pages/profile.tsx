import type { NextPage } from "next";
import Card from "../components/Card";
import Layout from "../components/Layout";

const ProfilePage: NextPage = () => {
  return (
    <Layout>
      <Card>Displays user profile page</Card>
    </Layout>
  );
};

export default ProfilePage;
